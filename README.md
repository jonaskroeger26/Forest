# City Focus

City Focus is a productivity app concept for Android + web where successful focus sessions grow a city.  
In strict mode, opening a blocked app (for example Snapchat) immediately fails the active session.

## Workspace

- `mobile/`: Flutter app (Android + web)
- `functions/`: Firebase Cloud Functions for reward validation
- `docs/`: balancing and beta planning docs
