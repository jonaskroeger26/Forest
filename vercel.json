{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable ../flutter && export PATH=\"$PATH:../flutter/bin\" && flutter config --enable-web && cd mobile && flutter pub get && flutter build web --release",
  "outputDirectory": "mobile/build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
